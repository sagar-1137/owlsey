// Twitter card image — Next.js auto-serves this at /twitter-image
// Reuses the same premium OG image for consistency
export { default, alt, size, contentType } from "./opengraph-image";
export const dynamic = "force-static";
